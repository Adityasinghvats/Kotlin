class it {
//    In Kotlin, the it keyword is used as an implicit name for single-parameter
//    lambda expressions or anonymous functions.
//    It simplifies the syntax for these expressions by eliminating the need to explicitly declare
//    a name for that parameter.
//    { x: Int -> x * 2 }
//    { it * 2 }
//    val numbers = listOf(1, 2, 3, 4, 5)
//    numbers.forEach { println(it) }
}